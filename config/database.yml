default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5
  username: postgres
  host: localhost
  password: password

development:
  <<: *default
  database: onqueue_dev

test:
  <<: *default
  database: onqueue_test
